#!/bin/bash

exe() { [ -x "$(command -v "$1")" ]; }

! exe xdg-open && exec /usr/bin/open "$@"

if exe xdo; then
	id=$(xdo id)
	xdo hide "$id"
	xdo hide "$id"
	xdg-open "$@"
	xdo show "$id"
	xdo show "$id"
else
	xdg-open "$@" >/dev/null 2>&1
fi
